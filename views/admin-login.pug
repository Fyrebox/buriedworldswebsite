extends layout

block content
  main#content.admin-login
    form.admin-login__card(action="/admin/login" method="post")
      span.admin-kicker Buried Worlds VR
      h1 Admin
      p For the person who runs the site. If that is not you, the game is back the way you came.
      if error
        p.admin-alert(role="alert")= error
      label.admin-field
        span Password
        input(type="password" name="password" required autofocus autocomplete="current-password")
      button.btn.btn--primary(type="submit") Sign in
      a.admin-login__home(href="/") ← Return to the website
